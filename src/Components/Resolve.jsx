import React from "react";

const Resolve = () => {
  return (
    <div>
      <h1 className="font-bold">Resolve Task</h1>
      <p className="font-xs">No resolved tasks yet.</p>
    </div>
  );
};

export default Resolve;
